version: '3.2'

services:
    beacon-network-ui:
        container_name: beacon-network-ui
        build:
            context: .
            dockerfile: Dockerfile
        ports:
        - '8081:8080'
        restart: on-failure